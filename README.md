# DK-Script

Discord: `airy8947`

Szerver Discord: `https://discord.gg/ZYvZtTws`

Általam készített scriptek — mind ingyenes.

## Documentation

Comprehensive public API / component docs are in:

- `docs/index.md`

Covered resources:

- `cl_notify`
- `Dk-frakciobuy`
- `esx_legacy_vehicleshop`

## Using the resources

This repo currently contains the resources as `.rar` archives in the repository root. Extract each archive into your FiveM server `resources/` folder and `ensure` them in `server.cfg`.
